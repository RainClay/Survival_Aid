package com.survivalaid.features;

import carpet.api.settings.Rule;

public final class VisitorPlayersRule {
    @Rule(categories = {"survival", "survival_aid"})
    public static String survivalAidVisitorPlayers = "";
}
